# ============================================
# PE Intelligence Platform — Dependencies
# ============================================

# --- Core Framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20

# --- Database ---
sqlalchemy==2.0.36
alembic==1.14.1
asyncpg==0.30.0
psycopg2-binary==2.9.10
aiosqlite==0.20.0

# --- Data & Finance ---
yfinance==0.2.51
pandas==2.2.3
numpy==2.2.1
numpy-financial==1.0.0
scikit-learn==1.6.1

# --- Validation & Serialization ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- HTTP & Async ---
httpx==0.28.1
aiohttp==3.11.11
openai==1.59.6

# --- Export ---
openpyxl==3.1.5
reportlab==4.2.5
python-pptx==0.6.23
python-docx==1.2.0
# chardet est une dépendance TRANSITIVE de reportlab (pas utilisée directement
# par notre code) — épinglée ici car `requests` (transitif via openai/yfinance)
# émet un RequestsDependencyWarning cosmétique dès que chardet >= 6.0.0 (son
# check de compatibilité interne, jamais mis à jour pour chardet 6.x — voir
# requests/__init__.py::check_compatibility, l'échec est capturé et transformé
# en simple warning, jamais une erreur bloquante). Version certifiée
# compatible testée ici : 5.2.0.
chardet==5.2.0

# --- PDF Processing ---
pdfplumber==0.11.4

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1

# --- Utils ---
python-dotenv==1.0.1
loguru==0.7.3
tenacity==9.0.0
cachetools==5.5.0
